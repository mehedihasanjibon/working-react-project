import React from 'react';

const SingleCart = ({movie}) => {
    // console.log(props.movie);
    // const {movie} = props;
    // console.log(movie);

    return (
        <div>
                    
        </div>
    );
};

export default SingleCart;